# Bungee - signage display face, static (single weight).
# Upstream also ships Inline, Outline and Shade as separate files drawn on the
# same skeleton; Regular and Inline share an identical viewBox per glyph, so
# they stack as layers. Adding them is one 'face' line each.
title      = Bungee
font       = Bungee-Regular.ttf
codepoints = bungee.codepoints
metadata   = bungee_metadata.json
