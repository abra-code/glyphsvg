# Pictogrammers Material Design Icons (MDI).
#
# A single static face: the webfont declares no variation axes, so --weight and
# --fill have nothing to act on here and are reported as warnings if given.
# Codepoints live in Supplementary PUA-A (U+F0001..U+F1D17).
#
# Populated by ./download.py - the .ttf and generated files are not committed.

title      = Material Design Icons
license    = Pictogrammers Free License (Apache 2.0 terms)
kind       = icon
font       = materialdesignicons-webfont.ttf
codepoints = mdi.codepoints
metadata   = mdi_metadata.json
